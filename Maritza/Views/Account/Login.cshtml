@model  dtoLogin
@{
    ViewBag.Title = "Login";
}


@using (Html.BeginForm("Login", "Account", FormMethod.Post, new { @id = "frmLogin" }))
{
    <div class="card">
        <div class="card-body">
            <div class="row">
                <div class="col-12 ">
                    @Html.LabelFor(model=>model.UserName, new {@class= "form-label fw-bold d-block" })
                    @Html.TextBoxFor(model => model.UserName, new { @class = "col-6" })
                </div>
                <div class="col-12">
                    @Html.LabelFor(model => model.Password, new { @class = "form-label fw-bold d-block" })
                    @Html.TextBoxFor(model => model.Password, new { @class = "col-6" })
                </div>
            </div>
        </div>
    </div>
}
